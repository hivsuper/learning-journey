spring:
  mail:
    username: user
    password: admin
    host: 127.0.0.1
    port: 3025
    protocol: smtp
    properties:
      "mail.smtp.auth": false
      "mail.smtp.starttls.enable": false
      "mail.smtp.starttls.required": false