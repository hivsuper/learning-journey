spring:
  test:
    database:
      replace: NONE

logging:
  level:
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} %X{key:-} - %msg%n"

wx:
  appId: 111
  secret: 222